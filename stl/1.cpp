
#include <iostream>
#include <algorithm>
#include <vector>
#include <map>

using namespace std;

typedef vector<int> vi;
typedef vector<vi> vii;
#define all(c) (c).begin(), (c).end()
#define tr(c,x) for(typeof((c).begin()) x=(c).begin();x!=(c).end();x++)

   vector<vector<int> > aa(10);

   int a[] = { 1, 2, 3 };
   aa[0].assign(a,a+3);
int main(){
   return 0;
}
